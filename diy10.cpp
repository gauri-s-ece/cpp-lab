#include <iostream>
using namespace std;
int main()
{
    int n;
    cout<<"enter the number:";
    cin>>n;
    if(n%2==0)
    cout<<"is divisible by 2.";
    else
    cout<<"is not divisible by 2.";
    return 0;
}